server {
    listen 80;
    # Replace "_" with rewndly.com on the VPS when DNS is ready.
    server_name _;

    server_tokens off;
    client_max_body_size 10m;
    resolver 127.0.0.11 valid=30s ipv6=off;

    set $api_upstream http://api:8080;
    set $frontend_upstream http://frontend:80;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /api/ {
        proxy_pass $api_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location /health {
        proxy_pass $api_upstream/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /swagger {
        return 404;
    }

    location / {
        proxy_pass $frontend_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS block for controlled trial deploys with an existing certificate.
# Replace "_" and certificate paths before enabling this block, or manage TLS
# on the host Nginx and keep this container on HTTP behind the host proxy.
#
# server {
#     listen 443 ssl http2;
#     server_name _;
#
#     ssl_certificate /etc/letsencrypt/live/rewndly.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/rewndly.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
#
#     server_tokens off;
#     client_max_body_size 10m;
#     resolver 127.0.0.11 valid=30s ipv6=off;
#
#     set $api_upstream http://api:8080;
#     set $frontend_upstream http://frontend:80;
#
#     location /api/ {
#         proxy_pass $api_upstream;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /health {
#         proxy_pass $api_upstream/health;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /swagger {
#         return 404;
#     }
#
#     location / {
#         proxy_pass $frontend_upstream;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
